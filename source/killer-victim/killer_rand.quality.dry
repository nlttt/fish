name: killer
on-arrival:
  var min = 0;
  var max = 5;
  k_num = Math.floor(Math.random() * (max - min + 1)) + min;
k_list = ['Russia', 'knife', 'gun', 'poison', 'bat', 'sword']
killer = k_list(this.state.k_num);
